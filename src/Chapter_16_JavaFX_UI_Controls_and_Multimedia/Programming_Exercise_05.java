package Chapter_16_JavaFX_UI_Controls_and_Multimedia;
import java.util.Scanner;

/**
 * Convert numbers
 * Write a program that converts between decimal, hex, and binary numbers, as shown in Figure 16.37c. 
 * When you enter a decimal value in the decimal value text field and press the Enter key, its corresponding hex and binary numbers are displayed in the other two text fields. 
 * Likewise, you can enter values in the other fields and convert them accordingly. 
 * (Hint: Use the Integer.parseInt(s, radix) method to parse a string to a decimal and use Integer.toHexString(decimal) and Integer.toBinaryString(decimal) to obtain a hex number or a binary number from a decimal.)
 * 
 * 
 * @author kevgu
 *
 */

public class Programming_Exercise_05
{
	Scanner input = new Scanner(System.in);
	public static void main(String[] args) 
	{
		
	}
}
